{% extends "backoffice/layout.html.twig" %}

{% block title %}
    Nouvelle saison |
    {{parent()}}
{% endblock %}

{% block content %}
    <h2>
        Nouvelle saison -
        {{ tvShow.title }}
    </h2>
    {{form_start(formView)}}
    {{form_row(formView.seasonNumber)}}
    {{form_row(formView.publishedAt)}}
    <button class="btn btn-primary">Valider</button>
    {{form_end(formView)}}

{% endblock %}
